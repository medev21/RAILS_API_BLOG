<div ng-controller = "MainCtrl">

  <form ng-submit = "addPost(newPost)">
    <input type="text" ng-model = "newPost.title" placeholder="place post title" class="form-control" autofocus>
    <input type="text" ng-model = "newPost.description" placeholder="place post description" class="form-control" autofocus>
    <input type="Submit" value="Add post">
  </form>

  <!-- <ul>
    <li ng-disabled="{{$first}}" ng-repeat = "post in posts track by post.id">
      <ul>
        <li>{{post.title}}</li>
        <li>{{post.description}}</li>
        <li><a class="btn btn-default" ng-click = "editing = !editing">Edit</a></li>
        <li><a class="btn btn-danger" ng-click="removePost($index)">Remove</a></li>
      </ul>
    </li>
  </ul> -->

  <div class="" ng-repeat = "post in posts track by post.id">
    <label ng-hide = "editing" >{{post.title}}</label>
    <input type="text" ng-show = "editing" ng-model="post.title">
    <div class="actions">
      <a class="btn btn-default" ng-click = "editing = !editing">Edit</a>
      <a class="btn btn-danger" ng-click="removePost($index)">Remove</a>
    </div>
  </div>

</div>
